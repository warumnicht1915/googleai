# Changelog

## 1.1.0

화질과 결과 수를 중심으로 한 개선입니다.

### 화질
- 미리보기를 Google 썸네일이 아니라 **원본 이미지**에서 생성합니다. 원본이 거부될 때만 썸네일로 대체합니다.
- 1600px 이하의 작은 원본은 재인코딩 없이 바이트 그대로 보관합니다. 큰 이미지만 긴 변 1600px, 품질 92로 줄이고 투명도는 PNG로 유지합니다.
- 카드와 확대 보기를 화면 픽셀 비율(devicePixelRatio)에 맞춰 렌더링합니다. Retina 화면에서 절반 해상도로 보이던 문제를 해결했습니다.
- 작은 원본을 1.4배 넘게 확대하지 않습니다.
- 다운로드가 끝나면 그 파일에서 미리보기를 다시 만들어 네트워크 재요청을 없앴습니다.

### 검색 결과
- 결과 목록 끝으로 스크롤하면 Google의 다음 이미지들을 이어서 수집합니다. **더 불러오기** 버튼도 추가했습니다.
- 한 검색에서 최대 500장까지 모으며, 이미 가져온 URL은 중복으로 추가하지 않습니다.
- 더 이상 새 결과가 없으면 상태 메시지로 알리고 버튼을 숨깁니다.
- SerpApi 경로도 `ijn` 페이지 파라미터로 다음 페이지를 요청합니다.
- 화면에 보이는 카드만 미리보기를 요청하는 지연 로딩을 넣었습니다.

### 디자인
- 텍스트 문자로 그리던 아이콘을 [Lucide](https://lucide.dev) SVG 세트로 교체했습니다 (ISC License).
- 좋아요 팝 애니메이션과 떠오르는 하트, 저장 중 회전 스피너와 완료 팝, 이미지 페이드인을 추가했습니다.
- **라이트 / 다크 테마 전환**을 추가했습니다 (사이드바 버튼 또는 Ctrl+T). 선택은 로컬 DB에 저장되어 다음 실행에도 유지됩니다.
- 사이드바에서 이전 위젯이 새 위젯 위에 남아 겹쳐 보이던 버그를 고쳤습니다.
- 카드에 원본 해상도를 함께 표시합니다.

### 기타
- 자동 테스트 17개 → **40개**.
- macOS 앱을 태그에서 빌드해 릴리스에 첨부하는 GitHub Actions 워크플로를 추가했습니다.

알려진 제한: Google CAPTCHA·차단·페이지 구조 변경 시 자동 검색이 제한될 수 있습니다. SerpApi는 개인 API 키가 필요한 선택 사항입니다. macOS 앱은 Apple 공증을 받지 않았습니다. 자세한 검증 범위는 `docs/VALIDATION.md` 를 확인하세요.

## 1.0.1

첫 공개 배포용 버전입니다.

- 하단 키워드 입력과 Google 이미지 자동 표시
- 별도 결과 가져오기 버튼 제거, 확인 후 자동 수집
- 검색 지연 시 자동 팝업 대신 메인 화면 안내
- 좋아요, 원본 다운로드, 다중 카테고리, 오프라인 라이브러리
- 최근 검색, 목록 필터, 확대 미리보기, 원본 출처
- 개인 데이터와 분리된 macOS Intel 배포 번들
